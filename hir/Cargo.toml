[package]
name = "hir"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
string_cache = "0.8.8"

[build-dependencies]
string_cache_codegen = "0.5.4"

[package.metadata.cargo-machete]
ignored = ["string_cache"]
